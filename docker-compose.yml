version: '3.8'

services:
  calendar:
    build: .
    container_name: dj-calendar
    ports:
      - "80:80"
      - "5000:5000"
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
    volumes:
      - ./logs:/var/log/nginx
    networks:
      - calendar-network

networks:
  calendar-network:
    driver: bridge 